
spring:
  profiles: default
  data:
    mongodb:
      host: mongo
      port: 27017
      database: raptor
#    username: admin
#    password: admin

server:
  contextPath: /profile
  port: 8070
  address: 0.0.0.0
  error:
    whitelabel:
      enabled: true

logging:
  level:
#    root: INFO
#    org.springframework.security: DEBUG
    org.createnet.raptor.profile: DEBUG
    org.createnet.raptor: DEBUG
