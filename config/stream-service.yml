
spring:
  devtools:
    restart:
      enabled: true
      trigger-file: raptor-stream-1.0.jar
      additional-paths: target
  profiles: default
  data:
    mongodb:
      host: mongo
      port: 27017
      database: raptor

server:
  contextPath: /stream
  port: 8080
  address: stream
  error:
    whitelabel:
      enabled: true

logging:
  level:
#    root: INFO
#    org.springframework.security: DEBUG
    org.createnet.raptor.profile: DEBUG
    org.createnet.raptor: DEBUG
    org.springframework.data.mongodb.core.index: OFF

raptor:
    url: http://rproxy
    auth:
        header: Authorization
        headerPrefix: Bearer
